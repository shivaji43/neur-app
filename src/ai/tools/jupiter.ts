export const jupiterTools = {
    // listTokens: {
    //     description: 'List verified tokens on Solana',
    //     parameters: z.object({ query: z.string() }),
    //     execute: async ({ query }: { query: string }) => {
    //         const result = await fetch(`/api/jupiter/tokens?q=${query}`)
    //         const data = await result.json()
    //         return data.tokens[0].address
    //     }
    // },
}